name: Lightning

on:
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [
          ubuntu-latest,
          macos-latest,
          windows-latest
        ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: visualdoj/setup-qemu@v1
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: master

      - run: zig env
      - run: zig build build_all -Dci --verbose
      - run: zig build build_all_tests -Dci --verbose
      - run: zig build test_all -Dci --verbose

